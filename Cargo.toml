[package]
name = "spatial_hash"
version = "0.1.0"
edition = "2021"

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3

[dependencies]
bevy = "0.11.0"
rand = "0.8.5"
# https://github.com/Warhorst/pad
pad = { path = "../pad" }